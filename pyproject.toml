[project]
name = "trace-rag-pipeline"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "langchain>=1.3.14",
    "langchain-text-splitters>=1.1.2",
    "pytest-playwright>=0.8.0",
]
